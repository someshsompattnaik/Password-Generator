# Password-Generator
This repository contains a Bash script designed to generate strong and customizable passwords. Users can easily specify the desired length and character sets (uppercase, lowercase, digits, special characters) via command-line arguments.
